import { LiferayDirective } from './liferay.directive';

describe('LiferayDirective', () => {
  it('should create an instance', () => {
    // const directive = new LiferayDirective();
    // expect(directive).toBeTruthy();
  });
});
